#pragma once
#include <Logger/Logger.h>
#include <glm/vec3.hpp>

namespace s_Renderer
{

    enum Platform
    {
        LINUX,
        WINDOWS,
        MACOS,
        //  MOBILE
    };
    enum Renderer
    {
        OpenGL
    };

}