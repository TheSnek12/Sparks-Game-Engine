{
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "preLaunchTask": "Meson: Build all targets",
      "name": "Launch",
      "program": "${workspaceFolder}/builddir/Sparks-Game-Engine-Test",
      "args": [],
      "cwd": "${workspaceFolder}"
    },

  {
    "name": "(gdb) Launch",
    "type": "cppdbg",
    "preLaunchTask": "Meson: Build all targets",
    "request": "launch",
    "program": "${workspaceFolder}/builddir/Sparks-Game-Engine-Test",
    "args": [],
    "stopAtEntry": false,
    "cwd": "${fileDirname}",
    "environment": [],
    "externalConsole": false,
    "MIMode": "gdb",
    "setupCommands": [
        {
            "description": "Enable pretty-printing for gdb",
            "text": "-enable-pretty-printing",
            "ignoreFailures": true
        },
        {
            "description": "Set Disassembly Flavor to Intel",
            "text": "-gdb-set disassembly-flavor intel",
            "ignoreFailures": true
        }
    ]
  }
  ]
}